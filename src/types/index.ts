export type menuType = {
    text: string,
    path: string
}

export type DateType = {
    day: number,
    month: number,
    year: number
}

export type TicketType = {
    type: string,
    amount: number,
    date: string,
    name: string,
    phone: number,
    email: string,
}